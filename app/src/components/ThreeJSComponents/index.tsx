import {GLTFLoader} from './GLTFLoader';
import {OrbitControls} from './OrbitControls';

/**
 * Just copy pasted the GLTFLoader and OrbitControls code from the source
 * Because for some reason importing from source does not work with Remix
 */

export {GLTFLoader, OrbitControls};