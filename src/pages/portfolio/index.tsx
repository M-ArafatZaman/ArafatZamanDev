import React from 'react';
// @mui components
import {
    Box
} from '@mui/material';

/* The portfolio page */
const Portfolio: React.FC = () => {

    return (
        <Box>
            Portfolio
        </Box>
    );
};

export default Portfolio;